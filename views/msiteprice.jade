extend layoutm

block header
  // header section
  section.header-section
    span.title #{user.firstname} #{user.lastname}
    .container
      ul
        li
          span 1. #{__('Price offers')}
        li
          a(href='/confirmation/#{parcel.id}?m=1') 2. #{__('Confirmation')}
        li
          a(href='#') 3. #{__('Payment')}
        li
          a(href='/dashboard?m=1') 4. #{__('Dashboard')}

block content
  // content
  section.section
    h1 #{__('Price Offers')}
    .container
      .form-sizes_scroll
        .scroll-size
          if drivers.cheapest.available
            article.offers
              figure
                img(src='/img/price_cheap.png', alt='')
                if drivers.cheapest.available
                  figcaption #{__('FROM')} &euro;#{drivers.cheapest.price}
                else
                  figcaption #{__('Not found')}
              strong #{__('cheapest')}
              ul
                li #{__('Expected dates:')}
                //li #{__('Departure:')} #{drivers.cheapest.pickupDate}
                //li #{__('Arrival:')} #{drivers.cheapest.arriveDate}
                li #{__('Departure:')} #{moment(parcel.pickupDate).format('LL')}
                li #{__('Arrival:')} #{moment(parcel.arriveDate).format('LL')}
              //a.more(href='#')
                i.icons-plus
                | #{__('MORE INFO')}
              a.orange_button(href='/confirmation/#{parcel.id}?driverId=#{drivers.cheapest.id}&m=1&delivery=#{drivers.cheapest.delivery}')
                | #{__('GET IT')}
                br
                | #{__('NOW')}
            //else
              a.orange_button(href='#')
                | #{__('NOT FOUND')}
          if drivers.fastest.available
            article.offers
              figure
                img(src='/img/price_fast.png', alt='')
                if drivers.fastest.available
                  figcaption #{__('FROM')} &euro;#{drivers.fastest.price}
                else
                  figcaption #{__('Not found')}
              strong #{__('fastest')}
              ul
                li #{__('Expected dates:')}
                //li #{__('Departure:')} #{drivers.fastest.pickupDate}
                //li #{__('Arrival:')} #{drivers.fastest.arriveDate}
                li #{__('Departure:')} #{moment(parcel.pickupDate).format('LL')}
                li #{__('Arrival:')} #{moment(parcel.arriveDate).format('LL')}
              //a.more(href='#')
                i.icons-plus
                | #{__('MORE INFO')}
              a.orange_button(href='/confirmation/#{parcel.id}?driverId=#{drivers.fastest.id}&m=1&delivery=#{drivers.fastest.delivery}')
                | #{__('GET IT')}
                br
                | #{__('NOW')}
            //else
              a.orange_button(href='#')
                | #{__('NOT FOUND')}
          if drivers.custom1.available
            article.offers
              figure
                img(src='/img/price_eco.png', alt='')
                if drivers.custom1.available
                  figcaption #{__('FROM')} &euro;#{drivers.custom1.price}
                else
                  figcaption #{__('Not found')}
              strong #{drivers.custom1.title}
              ul
                li #{__('Expected dates:')}
                //li #{__('Departure:')} #{drivers.custom1.pickupDate}
                //li #{__('Arrival:')} #{drivers.custom1.arriveDate}
                li #{__('Departure:')} #{moment(parcel.pickupDate).format('LL')}
                li #{__('Arrival:')} #{moment(parcel.arriveDate).format('LL')}
              //a.more(href='#')
                i.icons-plus
                | #{__('MORE INFO')}
              a.orange_button(href='/confirmation/#{parcel.id}?driverId=#{drivers.custom1.id}&m=1&delivery=#{drivers.custom1.delivery}')
                | #{__('GET IT')}
                br
                | #{__('NOW')}
            //else
              a.orange_button(href='#')
                | #{__('NOT FOUND')}
          if drivers.custom2.available && (!drivers.fastest.available || !drivers.cheapest.available)
            article.offers
              figure
                img(src='/img/price_eco.png', alt='')
                if drivers.custom2.available
                  figcaption #{__('FROM')} &euro;#{drivers.custom2.price}
                else
                  figcaption #{__('Not found')}
              strong #{drivers.custom2.title}
              ul
                li #{__('Expected dates:')}
                li #{__('Departure:')} #{moment(parcel.pickupDate).format('LL')}
                li #{__('Arrival:')} #{moment(parcel.arriveDate).format('LL')}
                i.icons-plus
                | #{__('MORE INFO')}
              a.orange_button(href='/confirmation/#{parcel.id}?driverId=#{drivers.custom2.id}&m=1&delivery=#{drivers.custom2.delivery}')
                | #{__('GET IT')}
                br
                | #{__('NOW')}
          if drivers.custom3.available && (!drivers.fastest.available || !drivers.cheapest.available)
            article.offers
              figure
                img(src='/img/price_eco.png', alt='')
                if drivers.custom3.available
                  figcaption #{__('FROM')} &euro;#{drivers.custom3.price}
                else
                  figcaption #{__('Not found')}
              strong #{drivers.custom3.title}
              ul
                li #{__('Expected dates:')}
                li #{__('Departure:')} #{moment(parcel.pickupDate).format('LL')}
                li #{__('Arrival:')} #{moment(parcel.arriveDate).format('LL')}
                i.icons-plus
                | #{__('MORE INFO')}
              a.orange_button(href='/confirmation/#{parcel.id}?driverId=#{drivers.custom3.id}&m=1&delivery=#{drivers.custom3.delivery}')
                | #{__('GET IT')}
                br
                | #{__('NOW')}
          .clear
      if more
        a.offers-more(href='#') #{__('SEE MORE OFFERS')}
